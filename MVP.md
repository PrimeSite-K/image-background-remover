# Background Remover — MVP 需求文档

## 1. 产品定位

**一句话**：上传图片，一键生成白底图，免费可用，高清付费下载。

**目标用户**：
- 电商卖家（亚马逊、独立站主图需要白底）
- 普通用户（证件照、社交媒体素材）

**核心差异化**：专注白底图输出，流程极简，无需注册即可试用。

---

## 2. 功能范围（MVP）

### 必须有（Must Have）

| 功能 | 说明 |
|------|------|
| 图片上传 | 支持拖拽 / 点击上传，格式：JPG、PNG、WEBP，最大 12MB |
| 自动抠图 | 调用 remove.bg API，去除背景 |
| 白底合成 | 前端 Canvas 合成纯白背景，输出 JPG |
| 预览对比 | 原图 vs 白底图并排展示 |
| 下载 | 免费用户下载低分辨率（800px），付费用户下载原始分辨率 |
| 用量限制 | 免费用户每天限 3 张（基于 IP 或 Cookie） |

### 不做（Out of Scope for MVP）

- 用户注册 / 登录
- 批量处理
- 自定义背景色 / 背景图
- 历史记录
- API 接口对外开放

---

## 3. 用户流程

```
访问首页
  → 上传图片（拖拽或点击）
  → 显示处理中动画
  → 展示原图 + 白底图对比
  → 点击下载
      ├─ 免费用户 → 下载 800px 版本
      └─ 付费用户 → 下载原始分辨率
```

---

## 4. Freemium 设计

| 功能 | 免费 | 付费 |
|------|------|------|
| 每日处理张数 | 3 张 | 无限 |
| 下载分辨率 | 800px | 原始分辨率 |
| 水印 | 无 | 无 |
| 批量处理 | ✗ | ✓（后续版本） |

**付费方式**：Stripe，按月订阅（定价待定，参考竞品 $5-9/月）

---

## 5. 技术架构

```
前端：      Next.js 14 (App Router) + Tailwind CSS
API 代理：  Cloudflare Workers（保护 remove.bg API Key）
抠图服务：  remove.bg API
白底合成：  浏览器 Canvas API（前端完成，无需后端）
存储：      无（图片全程在内存，不落盘）
部署：      Cloudflare Pages（前端）+ Cloudflare Workers（代理）
支付：      Stripe（MVP 后期接入）
```

**安全设计**：remove.bg API Key 存储在 Cloudflare Workers 环境变量中，前端不可见；Worker 通过 `ALLOWED_ORIGIN` 校验请求来源，防止盗用。

---

## 6. 页面结构

| 路由 | 内容 |
|------|------|
| `/` | 主工具页（上传 + 预览 + 下载） |
| `/pricing` | 定价页（MVP 后期） |

Landing page 即工具页，降低跳出率，用户进来直接能用。

---

## 7. 非功能需求

- **性能**：上传到出结果 < 5 秒（取决于 remove.bg API 响应）
- **兼容性**：支持 Chrome、Safari、Firefox 最新版；移动端可用
- **隐私**：图片不存储在服务器，处理完即丢弃

---

## 8. 成功指标（MVP 验证）

- 上线 2 周内：日活用户 > 50
- 抠图成功率 > 95%
- 免费 → 付费转化率目标：> 3%

---

## 9. 开发里程碑

| 阶段 | 内容 | 状态 |
|------|------|------|
| Phase 1 | 项目骨架 + 核心抠图流程 | ✅ 完成 |
| Phase 2 | 用量限制（IP/Cookie）+ 分辨率限制 | 待开发 |
| Phase 3 | Stripe 付费接入 | 待开发 |
| Phase 4 | SEO 优化 + Landing page 文案 | 待开发 |
